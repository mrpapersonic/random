// garf simulator
#include <iostream>
#include <stdio.h>
#include <string.h>

int main() {
    char answer[255];
    std::cout << "would you like to monetize 1123.best?: ";
    fgets(answer, sizeof(answer), stdin);
    if(answer == "yes") {
        std::cout << "it's a yes!";
    }
    else {
        std::cout << "it's a yes!";
    }
    return 0;
}
